// *** DO NOT EDIT. GENERATED BY PRISMIX. ***
datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Post {
  id          Int      @id @default(autoincrement())
  title       String
  post        String   @db.VarChar(200)
  isPublished Boolean  @default(false)
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt
  user_id     Int
  user        User     @relation(name: "PostToUser", fields: [user_id], references: [id])
}

model User {
  id       Int      @id @default(autoincrement())
  posts    Post[]   @relation(name: "PostToUser")
  username String   @unique
  role     UserRole @default(admin)
}

model Testing {
  id    Int    @id @default(autoincrement())
  title String
}

enum UserRole {
  client
  admin
}
